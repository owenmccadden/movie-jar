// components/Jar.js
import React from 'react';
import styles from '../styles/Jar.module.css';

const Jar = () => {
    return (
        <div className={styles.jar}>
            <img src="/images/jar.png" alt="Jar" className={styles.jarImage} />

        </div>
    );
};

export default Jar;
